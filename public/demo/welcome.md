# Welcome

This is a real demo file served from `public/demo/welcome.md`, used when Motion
runs in a browser (outside the Tauri shell) so the web/dev storage path has
something genuine to read instead of mock text.
